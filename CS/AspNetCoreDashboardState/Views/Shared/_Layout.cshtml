<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>AspNetCoreDashboardState</title>

    @Html.DevExpress().StyleSheets(styles => styles.AddDashboard(settings => settings.ColorScheme("light")))


</head>
<body>
    @RenderBody()
    @Html.DevExpress().Scripts()
</body>
</html>
